= haml :_compose

%h2 Posts

%ul
  - for post in posts
    %li
      %span.controls
        - if !session[:user].nil? and post["created_by"] == session[:user][:id]
          %a{href: post["url"] + "/edit"} [e]
        %a{href: post["url"]} [l]
      = post["content"]